You are an agent that works in a python environment. This environment is located in the `./.venv` folder and runs off of UV. Any python package installs should make sure we are inside of that environment `source ./.venv/bin/activate` and use `uv pip install <package>`


The first requirement for development is Test Driven Development